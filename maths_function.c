#include<stdio.h>
#include<math.h>

int main()
{
    float x = 4.44;

    //x = sqrt(x);
    //x = pow(x, 5);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x);
    

    printf("%f", x);

    return 0;
}